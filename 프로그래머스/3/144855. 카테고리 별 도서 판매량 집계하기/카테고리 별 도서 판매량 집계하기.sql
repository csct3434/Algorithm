SELECT
    b.CATEGORY,
    SUM(s.SALES) AS TOTAL_SALES
FROM
    BOOK_SALES s
JOIN
    BOOK b
USING
    (BOOK_ID)
WHERE
    s.SALES_DATE LIKE '2022-01%'
GROUP BY 
    b.CATEGORY
ORDER BY
    b.CATEGORY